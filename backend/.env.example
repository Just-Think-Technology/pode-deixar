# -- Environment --
NODE_ENV=""
AUTH_PORT=3001
USERS_PORT=3002
SERVICE_ORDERS_PORT=3003

# -- Frontend --
FRONTEND_URL=""
ALLOWED_ORIGINS=""

# -- JWT --
JWT_ACCESS_SECRET=""
JWT_REFRESH_SECRET=""

# -- SMTP (Email) --
SMTP_HOST=""
SMTP_PORT=
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# -- Security --
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# -- Database (Neon) --
DATABASE_URL=""
DIRECT_DATABASE_URL=""
