// purpose — JWT constants shared across signing, verification, and strategy
export const JWT_ISSUER = 'pode-deixar-auth';
export const JWT_AUDIENCE = 'pode-deixar';
export const JWT_ALGORITHMS = ['HS256'] as const;
