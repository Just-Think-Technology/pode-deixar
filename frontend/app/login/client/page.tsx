// purpose: Client login route — renders the client login form
import { ClientLoginForm } from "@/components/pages/auth-form-page";

export default function ClientLoginPage() {
    return <ClientLoginForm />;
}
