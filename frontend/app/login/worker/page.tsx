// purpose: Worker login route — renders the worker login form
import { WorkerLoginForm } from "@/components/pages/auth-form-page";

export default function WorkerLoginPage() {
    return <WorkerLoginForm />;
}
