// purpose: Role selection route — renders the user-type picker
import SelectUserPage from "@/components/pages/select-user-page";

export default function SelectUser() {
    return <SelectUserPage />;
}