// purpose: Worker registration route — renders the worker signup form
import { WorkerRegisterForm } from "@/components/pages/auth-form-page";

export default function WorkerRegisterPage() {
    return <WorkerRegisterForm />;
}
