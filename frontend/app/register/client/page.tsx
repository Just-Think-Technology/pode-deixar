// purpose: Client registration route — renders the client signup form
import { ClientRegisterForm } from "@/components/pages/auth-form-page";

export default function ClientRegisterPage() {
    return <ClientRegisterForm />;
}
